import postcss from 'postcss';

type PluginOptions = {
    base?: string;
    optimize?: boolean | {
        minify?: boolean;
    };
};
declare const _default: postcss.PluginCreator<PluginOptions>;

export = _default;
